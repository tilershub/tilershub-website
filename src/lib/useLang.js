// Sinhala is the site language for server rendering and client hydration.
// Ignore older English browser preferences so every visitor sees the same content.
export function useLang() { return 'si' }
